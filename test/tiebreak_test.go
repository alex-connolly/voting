package test

/*import (
	"testing"
	"demos/voting"
)

func TestChooseRandom(t *testing.T){
	vr := voting.VotingResult{true, []string{"a", "b"}}
	vr2 := voting.ChooseRandom(1, vr)
	if len(vr2.Values) != 1 {
		t.Error("wrong")
	}
	vr = voting.VotingResult{true, []string{"a", "b", "c"}}
	vr2 = voting.ChooseRandom(1, vr)
	if len(vr2.Values) != 1 {
		t.Error("wrong")
	}
	vr = voting.VotingResult{true, []string{"a", "b", "c"}}
	vr2 = voting.ChooseRandom(2, vr)
	if len(vr2.Values) != 2 {
		t.Error("wrong")
	}
	vr = voting.VotingResult{true, []string{"a", "b", "c"}}
	vr2 = voting.ChooseRandom(3, vr)
	if len(vr2.Values) != 3 {
		t.Error("wrong")
	}
	vr = voting.VotingResult{true, []string{"a", "b", "c"}}
	vr2 = voting.ChooseRandom(4, vr)
	if len(vr2.Values) != 4 {
		t.Error("wrong")
	}

}*/
